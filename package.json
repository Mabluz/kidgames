{
  "name": "memory",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "kill": "lsof -ti:8000 | xargs kill -9 2>/dev/null || true && lsof -ti:35729 | xargs kill -9 2>/dev/null || true",
    "start": "nodemon server.js",
    "serve": "node server.js"
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "connect-livereload": "^0.6.1",
    "livereload": "^0.10.3",
    "nodemon": "^3.1.10"
  }
}
